### 优化手段
1. `mat_transpose_f32x4_shared_col2row2d_kernel`: 这个kernel里，先将数据搬运到shm mem里，这里的搬运是可以合并的。后续在写入的时候，由于shm mem连续， <br>
所以也可以合并，这就是个合并访存。如果不这么做，每个线程直接：全局显存->寄存器->全局显存，这也可以完成功能，但是由于写入时的线程跳跃，会导致多个内存事务， <br>
访存无法合并，速度就慢了。虽然步骤多了，但是合并访存的收益更高。